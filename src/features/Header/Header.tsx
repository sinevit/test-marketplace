import React from 'react';
import { Wrapper } from './styled'

const Header: React.FC = () => {
    return <>
        <Wrapper>
            HEADER
        </Wrapper>
    </>
};

export default Header;
